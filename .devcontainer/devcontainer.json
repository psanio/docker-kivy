{
  "name": "kivy-android-4.1",
  "image": "ubuntu:18.04",
  "remoteUser": "root",
  "workspaceFolder": "/workspace",
  "postCreateCommand": "apt-get update && apt-get install -y --no-install-recommends build-essential git curl wget unzip zip ca-certificates software-properties-common gnupg2 lsb-release && echo 'base ready'",
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-python.python"
      ]
    }
  },
  "forwardPorts": [],
  "mounts": [
    "source=${localWorkspaceFolder},target=/workspace,type=bind,consistency=cached"
  ]
}

